overwrite: true
schema: ./test/**/*.graphql
generates:
  test/__generated__/graphql.ts:
    plugins:
      - "@graphql-codegen/typescript"
    config:
      useIndexSignature: true
      namingConvention: keep
